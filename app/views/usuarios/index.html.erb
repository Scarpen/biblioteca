<h1>Listando Usuarios</h1><br />
<div align="center">
<table border="1">
  <tr>
    <th width=30%>Nome</th>
    <th width=30>Dasta Nascimento</th>


  </tr>

<% @usuario.each do |t| %>
  <tr>
    <td><%= t.login%></td>
    <td><%= t.senha%></td>
    <td width=7%><%= link_to 'Mostrar', t %></td>
    <td width=7%><%= link_to 'Editar', edit_usuario_path(t) %></td>
    <td width=7%><%= link_to 'Apagar', t, method: :delete, data: { confirm: 'Tem certeza?' } %></td>
  </tr>
<% end %>
</table>

<br />

<div class="btn-group">
  <%= link_to new_autor_path do %>
  <button class="btn">Novo Autor</button>
  <% end %>
  <%= link_to endbiblis_path do%>
  <button class="btn">Lista Endereços</button>
  <% end %>
</div>